"""Seeders for the one consolidated relay.db (backend/data/relay.db).

  python -m backend.seed.seed_routing   # routing tables only
  python -m backend.seed.seed_p2p       # p2p tables only
  python -m backend.seed.seed_all       # everything → a fresh full db
"""
