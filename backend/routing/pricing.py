"""RESERVED — peer-to-peer exchange pricing (future product).

Age + condition depreciation pricing belongs in the P2P resale product, NOT in the
reverse-logistics routing module. Routing treats every return as a NEW unit (it arrives
inside the return window at full original value). See docs/ARCHITECTURE.md for the distinction.

Do NOT import from here in routing/router.py or routing/model/.
"""
