# Environment
.env
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# NOTE: the seeded DB (backend/data/relay.db) and the catalog images
# (backend/catalog_images/*.jpg) are intentionally COMMITTED so a hosted deploy ships with
# them — they are NOT ignored.

# Routing training data is regenerated; keep it out of git. The trained model .pkl IS
# committed (so a host uses XGBoost instead of the rule fallback).
backend/routing/model/training_data.csv

# Uploads / temp
uploads/
*.tmp

# IDE
.vscode/
.idea/
*.swp
