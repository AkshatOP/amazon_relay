fastapi>=0.110.0
uvicorn[standard]>=0.27.0
google-genai>=0.3.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.6.0

# --- Routing module ---
httpx>=0.27.0          # OSRM road-distance lookups (backend/routing/geo.py)
xgboost>=2.0.0         # routing decision model (run: python -m backend.routing.model.train_router)
scikit-learn>=1.4.0    # train/test split + metrics
pandas>=2.2.0          # training CSV loading
numpy>=1.26.0          # feature vectors
joblib>=1.3.0          # model (de)serialization

# Grading skill is a YAML file (loaded by backend/grading/grading_agent.py)
pyyaml>=6.0
